import React from 'react'
// import PageConfig from 'components/PageConfig'
// import { BatchImportDialogContent } from 'components'
// import { EmployeesPicker } from 'components'

const Test = () => {

  return <>
    {/* <button onClick={() => {
    global.$showDialog({
      maxWidth: "xs",
      children: (
        <BatchImportDialogContent

        />
      ),
      noDialogActions: false,
      title: `导入`,
    })
  }}>导入</button> */}
    {/* <button onClick={() => {
      global.$showDialog({
        maxWidth: "lg",
        children: (
          <EmployeesPicker

          />
        ),
        noDialogActions: false,
        title: `查找员工`,
      })
    }}>查找员工</button> */}
  </>
}

export default Test